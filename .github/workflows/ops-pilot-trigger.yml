name: OpsPilot hourly trigger

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  wake-and-trigger:
    runs-on: ubuntu-latest
    env:
      MOCK_URL: ${{ secrets.OPSPILOT_MOCK_URL }}
      AGENT_URL: ${{ secrets.OPSPILOT_AGENT_URL }}
      RENDER_BACKEND_URL: ${{ secrets.OPSPILOT_RENDER_BACKEND_URL }}
    steps:
      - name: Wake mock server
        run: |
          for i in $(seq 1 6); do
            if curl -fs "${MOCK_URL}/health"; then
              echo "Mock server awake"
              break
            fi
            echo "Waiting for mock server..."
            sleep 10
          done

      - name: Wake backend render service
        run: |
          if [ -z "${RENDER_BACKEND_URL}" ]; then
            echo "RENDER_BACKEND_URL is not configured, skipping backend wake step."
            exit 0
          fi

          for i in $(seq 1 6); do
            if curl -fs "${RENDER_BACKEND_URL}/health"; then
              echo "Render backend awake"
              break
            fi
            echo "Waiting for Render backend..."
            sleep 10
          done

      - name: Wake agent server
        run: |
          for i in $(seq 1 6); do
            if curl -fs "${AGENT_URL}/health"; then
              echo "Agent server awake"
              break
            fi
            echo "Waiting for agent server..."
            sleep 10
          done

      - name: Trigger OpsPilot pipeline
        run: |
          curl -X POST "${AGENT_URL}/trigger" \
            -H "Content-Type: application/json" \
            -d '{"incident_context":"Hourly scheduled monitoring check"}'
